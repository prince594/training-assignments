const dirname = process.cwd();
const ourMail = 'prince@newput.com';
const subject = 'Welcome to My Organisation';
const loginError = 'You are not loggedin! Please login to get access';
const verifyError = 'Access Denied! Unauthorized User';
const noCredentialError = "Please provide email and password";
const invalidCredentialError = "Please provide valid user credential"

export default {dirname, ourMail, subject, loginError, verifyError, noCredentialError, invalidCredentialError}; 